import styled from "styled-components"

export const Container = styled.div`
    margin: 0;
    padding: 0;
    height: 100vh;
    display: flex;
    flex-direction: column;
    background: #dbdbdb;
    text-align: center;
    position: relative;

    & canvas {
        flex: 1;
    }

`